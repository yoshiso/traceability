require 'spec_helper'

describe Traceability do
  it 'has a version number' do
    expect(Traceability::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
